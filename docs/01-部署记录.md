# 部署记录

## 2026-06-14

### 工作区

- 工作目录：`C:\Users\龙城\Documents\让 AI 替你刷 B 站`
- Git 分支：`codex/bilibili-ai-workspace`
- 当前仓库原本为空仓库，无初始提交。

### 源码获取

`git clone` 直连 GitHub 失败：

```text
Failed to connect to github.com port 443
```

改用 GitHub codeload zip 下载并解压：

- `jackwener/bilibili-summary` -> `bilibili-summary`
- `chenluQwQ/bilibili-ai-bot` -> `bilibili-ai-bot`

### 依赖安装

`bilibili-ai-bot`：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r Requirements.txt
```

结果：安装成功。

`bilibili-summary`：

默认 PyPI 多次下载断包，改用清华源：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
```

结果：安装成功。

### 本地安全包装

新增：

- `tools/check_bilisummary.py`
- `tools/bilibili_ai_dry_run.py`
- `tools/fetch_bilibili_video_draft.py`
- `bilibili-summary/.env.example`
- `bilibili-ai-bot/config.dry-run.example.json`

### Git 管理

总仓库只管理本项目自己的说明、规则、工具脚本和草稿输出。两个外部项目目录已加入 `.gitignore`：

- `bilibili-summary/`
- `bilibili-ai-bot/`

它们作为本地部署目录保留在工作区，不混进总仓库提交。

### 已验证

- BiliSummary 依赖导入成功。
- bilibili-ai-bot 依赖导入成功。
- `tools/fetch_bilibili_video_draft.py` 已用公开视频生成总结、选题和评论草稿。
- `bilibili-ai-bot/config.json` 已覆盖为 dry-run 配置，主动评论、动态和主动行为均关闭。
- 未配置真实 B站 Cookie。
- 未调用真实互动接口。
